using PhoneBook.UI;

MainMenu mainMenu = new MainMenu();
while (true)
{
    var choice = mainMenu.ShowMainMenu();
    if (choice == 4)
    { break; }

    switch (choice)
    {
        case 1:
            break;
        case 2:
            break;
        case 3:
            break;
    }
}